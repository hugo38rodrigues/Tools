# Init
You do have eslint in your project

# Install Prettier

For install Prettier, add plugin <prettier> in vscode and add a **.prettierrc** & **.prettierignore** file in root project. 

Next install this package: **prettier** **eslint-config-prettier**

