# Install the Linter for React

Copy and paste the following command to install the dependencies, then paste **.eslintrc.react.cjs** into your repo.

```
npm i -D eslint-config-airbnb eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-jsx-a11y eslint-plugin-import
```
