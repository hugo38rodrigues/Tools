# Init 
You do install they packages: ```npm install -D typescript@6 tsx @types/node```
Next if you use **express**, **cors** **JWT**, ...etc add this packages: ```npm install -D @types/express @types/cors @types/jsonwebtoken @types/pg```

Finally you do copy this file **tsconfig.json** into your repository