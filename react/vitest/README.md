# Install
```
  npm install -D vite @vitejs/plugin-react vitest jsdom \
  @testing-library/react @testing-library/jest-dom @testing-library/user-event
```
next you do copy this file **vite.config.ts** into your project